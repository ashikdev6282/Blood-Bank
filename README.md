# React + Vite

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

Currently, two official plugins are available:

- [@vitejs/plugin-react](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react/README.md) uses [Babel](https://babeljs.io/) for Fast Refresh
- [@vitejs/plugin-react-swc](https://github.com/vitejs/vite-plugin-react-swc) uses [SWC](https://swc.rs/) for Fast Refresh




# 🩸 Blood Bank Management System (React + Firebase)

A full-stack Blood Bank Management System built with **React**, **Firebase Auth**, and **Cloud Firestore**.  
It includes separate **User** and **Admin** portals with live data, role-based access, and a modern UI.

---

## 🚀 Features

### 👤 User Portal
- User registration & login (Firebase Authentication)
- Profile page with editable details and photo upload
- Donor registration (mark user as donor)
- Request blood with detailed form & validations
- Host a blood drive (only for registered donors)
- View hosted drive history
- Responsive UI with Bootstrap + animations

### 🛡 Admin Portal
- Admin login (Firebase Auth)
- Dashboard with overview stats
- Manage **blood stock** (add / edit / delete)
- Manage **blood requests** (approve / reject)
- Manage **donors** (view profile, block, remove)
- Manage **blood drives** (approve / complete)
- Real-time data updates from Firestore

---

## 🧱 Tech Stack

### Frontend
- React (Vite)
- React Router
- React Context API
- React Bootstrap / Bootstrap
- AOS (animations)
- Lottie animations
- React Icons

### Backend / DB
- Firebase Authentication
- Cloud Firestore
- Firebase Storage (optional)
- Firebase Hosting / Vercel Hosting

---



